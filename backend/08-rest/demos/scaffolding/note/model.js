'use strict';

const uuid = require("uuid/v1");

class Note {
    
    constructor(name) { 
        // generate an id
        // title
        // date
        // content
    }
    
    // Instance (prototype) Methods
    toString() { 
    }
    
}

module.exports = Note;